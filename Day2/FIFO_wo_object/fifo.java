
public class fifo {
	public static int enfile (int tab[], int val) {
		if(tab == null) {
			tab[0]=val;
		}
		else {
			tab[tab.length+1]=val;
		}
		return (tab.length);
	}
	
	public static void defile (int tab[]) {
	if (tab==null)
		return null;
	else
	{
	
	}
	}
}

