
public class FIFO_wo_object {
	int[] elements;
	int size;
	
	public static int enfile(int val, int tab[]) {
		if (tab == 0) {
			int file[0] = val 
		} else {
			int[tab.length + 1] file;
			file[file.length]=val;
		}
	}
	
	public static int pop(Fifo liste) {
		int issou = f.elements[0];
		size = size-1;
		
		return liste.elements[0];
	}
}
